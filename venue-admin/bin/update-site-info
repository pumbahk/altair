#!/bin/sh

CURRENT=$(dirname $0)

SITE=$1
PREF=$2
SUB_NAME=$3

echo "UPDATE Site,Venue SET Site.prefecture=UNHEX(\"${PREF}\"),Venue.sub_name=UNHEX(\"${SUB_NAME}\") WHERE Site.id=${SITE} and Site.id=Venue.site_id and Site.deleted_at is null and Venue.deleted_at is null" | \
$CURRENT/db-master
