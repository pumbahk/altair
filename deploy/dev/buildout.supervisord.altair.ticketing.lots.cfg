[supervisor]
programs +=
    10 altair.ticketing.lots.1 ${buildout:directory}/bin/pserve [${conf:path}/altair.ticketing.lots.ini http_port=9121 altair.findable_label.label=${altair:findable_label.label} altair.findable_label.background_color=${altair:findable_label.background_color}] ${buildout:directory}/var/altair/ticketing
