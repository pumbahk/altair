[supervisor]
programs +=
    10 altair.ticketing.whattime (autostart=false) ${buildout:directory}/bin/pserve [ ${altair:conf_path}/altair.ticketing.whattime.ini http_port=9031 www_check_docroot=${buildout:directory}/var/www-check/altair.ticketing.whattime altair.findable_label.label=${altair:findable_label.label} altair.findable_label.background_color=${altair:findable_label.background_color} session.type=${altair:beaker_backend_type} cache.type=${altair:beaker_backend_type} altair.console_loglevel=${altair:console_loglevel} ] ${buildout:directory}/var/altair/ticketing

[mkvardirs]
paths += var/www-check/altair.ticketing.whattime
