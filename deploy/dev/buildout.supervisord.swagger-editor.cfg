[supervisor]
programs +=
    10 swagger-editor (autostart=${altair:supervisord.autostart}) node [ ${buildout:directory}/../../swagger-editor/server.js --port=8087 ] ${buildout:directory}/../../swagger-editor true
