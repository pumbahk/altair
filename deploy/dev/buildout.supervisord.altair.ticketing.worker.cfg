[supervisor]
programs +=
    10 altair.ticketing.lot_worker (autostart=false) ${buildout:directory}/bin/mserve [ ${altair:conf_path}/altair.ticketing.lot_worker.ini ] ${buildout:directory}/var/altair/ticketing true
