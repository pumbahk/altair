[supervisor]
programs +=
    10 altair.ticketing.booster.bigbulls (autostart=false) ${buildout:directory}/bin/pserve [ ${altair:conf_path}/altair.ticketing.booster.bigbulls.ini http_port=9083 www_check_docroot=${buildout:directory}/var/www-check/altair.ticketing.booster.bigbulls altair.findable_label.label=${altair:findable_label.label} altair.findable_label.background_color=${altair:findable_label.background_color} altair.console_loglevel=${altair:console_loglevel} ] ${buildout:directory}/var/altair/ticketing true

[mkvardirs]
paths += var/www-check/altair.ticketing.booster.bigbulls
