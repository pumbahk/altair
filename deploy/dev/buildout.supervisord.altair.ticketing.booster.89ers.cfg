[supervisor]
programs +=
    10 altair.ticketing.booster.89ers (autostart=false) ${buildout:directory}/bin/pserve [ ${altair:conf_path}/altair.ticketing.booster.89ers.ini http_port=9081 www_check_docroot=${buildout:directory}/var/www-check/altair.ticketing.booster.89ers altair.findable_label.label=${altair:findable_label.label} altair.findable_label.background_color=${altair:findable_label.background_color} altair.console_loglevel=${altair:console_loglevel} ] ${buildout:directory}/var/altair/ticketing true

[mkvardirs]
paths += var/www-check/altair.ticketing.booster.89ers
