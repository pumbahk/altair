[supervisor]
programs +=
    10 altair.ticketing.booster ${buildout:directory}/bin/pserve [${conf:path}/altair.ticketing.booster.ini http_port=9081 altair.findable_label.label=${altair:findable_label.label} altair.findable_label.background_color=${altair:findable_label.background_color}] ${buildout:directory}/var/altair/ticketing
