[supervisor]
programs +=
    10 altair.ticketing.whattime ${buildout:directory}/bin/pserve [${altair:conf_path}/altair.ticketing.whattime.ini http_port=${altair:ticketing.whattime.port} www_check_docroot=${buildout:directory}/var/www-check/altair.ticketing.whattime workers=10] ${buildout:directory}/var/altair/ticketing true

[mkvardirs]
paths += var/www-check/altair.ticketing.whattime
