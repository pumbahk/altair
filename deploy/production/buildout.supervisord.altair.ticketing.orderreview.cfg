[supervisor]
programs +=
    10 altair.ticketing.orderreview ${buildout:directory}/bin/pserve [${altair:conf_path}/altair.ticketing.orderreview.ini http_port=${altair:ticketing.orderreview.port} www_check_docroot=${buildout:directory}/var/www-check/altair.ticketing.orderreview workers=2] ${buildout:directory}/var/altair/ticketing true

[mkvardirs]
paths += var/www-check/altair.ticketing.orderreview
