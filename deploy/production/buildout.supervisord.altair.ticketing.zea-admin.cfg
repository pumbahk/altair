[supervisor]
programs +=
    10 altair.ticketing.zea-admin (startsecs=10) ${buildout:directory}/bin/pserve [ -n default ${altair:conf_path}/altair.ticketing.zea-admin.ini http_port=${altair:ticketing.zea-admin.port} www_check_docroot=${buildout:directory}/var/www-check/altair.ticketing.zea-admin workers=${altair:ticketing.zea-admin.workers}] ${buildout:directory}/var/altair/ticketing true

[mkvardirs]
paths += var/www-check/altair.ticketing.zea-admin
