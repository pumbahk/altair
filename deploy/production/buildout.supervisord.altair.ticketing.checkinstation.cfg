[supervisor]
programs +=
    10 altair.ticketing.checkinstation ${buildout:directory}/bin/pserve [${altair:conf_path}/altair.ticketing.checkinstation.ini http_port=${altair:ticketing.checkinstation.port} www_check_docroot=${buildout:directory}/var/www-check/altair.ticketing.checkinstation workers=2] ${buildout:directory}/var/altair/ticketing true

[mkvardirs]
paths += var/www-check/altair.ticketing.checkinstation
