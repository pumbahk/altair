[supervisor]
programs +=
    10 altair.ticketing.lot_worker ${buildout:directory}/bin/mserve [ ${altair:conf_path}/altair.ticketing.lot_worker.ini lots] ${buildout:directory}/var/altair/ticketing true
