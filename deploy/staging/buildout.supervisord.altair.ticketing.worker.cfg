[supervisor]
programs +=
    10 altair.ticketing.worker.lots (autostart=${altair:supervisord.autostart}) ${buildout:directory}/bin/mserve [ ${altair:conf_path}/altair.ticketing.worker.lots.ini lots.election lots.rejection] ${buildout:directory}/var/altair/ticketing true
    10 altair.ticketing.worker.cart (autostart=${altair:supervisord.autostart}) ${buildout:directory}/bin/mserve [ ${altair:conf_path}/altair.ticketing.worker.cart.ini cart] ${buildout:directory}/var/altair/ticketing true
