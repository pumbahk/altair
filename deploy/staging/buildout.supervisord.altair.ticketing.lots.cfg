[supervisor]
programs +=
    10 altair.ticketing.lots ${buildout:directory}/bin/pserve [${altair:conf_path}/altair.ticketing.lots.ini http_port=${altair:ticketing.lots.port} altair.findable_label.label=${altair:findable_label.label} altair.findable_label.background_color=${altair:findable_label.background_color} altair.console_loglevel=${altair:console_loglevel}] ${buildout:directory}/var/altair/ticketing
