[supervisor]
programs +=
    10 altair.ticketing.zea-admin (autostart=${altair:supervisord.autostart}) ${buildout:directory}/bin/pserve [ -n default ${altair:conf_path}/altair.ticketing.zea-admin.ini http_port=${altair:ticketing.zea-admin.port} www_check_docroot=${buildout:directory}/var/www-check/altair.ticketing.zea-admin altair.findable_label.label=${altair:findable_label.label} altair.findable_label.background_color=${altair:findable_label.background_color} altair.console_loglevel=${altair:console_loglevel} ] ${buildout:directory}/var/altair/ticketing true

[mkvardirs]
paths +=
    var/www-check/altair.ticketing.zea-admin
