[supervisor]
programs +=
    10 altair.ticketing.cart ${buildout:directory}/bin/pserve [${altair:conf_path}/altair.ticketing.cart.ini http_port=${altair:ticketing.cart.port} altair.findable_label.label=${altair:findable_label.label} altair.findable_label.background_color=${altair:findable_label.background_color} altair.console_loglevel=${altair:console_loglevel}] ${buildout:directory}/var/altair/ticketing
