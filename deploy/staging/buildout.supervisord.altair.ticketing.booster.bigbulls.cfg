[supervisor]
programs +=
    10 altair.ticketing.booster.bigbulls ${buildout:directory}/bin/pserve [${altair:conf_path}/altair.ticketing.booster.bigbulls.ini http_port=${altair:ticketing.booster.bigbulls.port} altair.findable_label.label=${altair:findable_label.label} altair.findable_label.background_color=${altair:findable_label.background_color} altair.console_loglevel=${altair:console_loglevel}] ${buildout:directory}/var/altair/ticketing
