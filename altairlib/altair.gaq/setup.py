from setuptools import setup, find_packages

setup(name="altair_gaq",
    install_requires=[
        "setuptools>0.7",
        "pyramid",
        "zope.interface",
        "altair.mobile",
    ],

    entry_points="""
    """
)
