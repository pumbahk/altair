<UserControl x:Class="QR.presentation.gui.control.InspectCase"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding CaseName, Mode=OneWay}" Background="AliceBlue"/>
        <TextBlock Grid.Row="1" Style="{StaticResource ErrorMessageForUser}" Text="{Binding ErrorMessage, Mode=OneWay}"></TextBlock>
    </Grid>
</UserControl>
