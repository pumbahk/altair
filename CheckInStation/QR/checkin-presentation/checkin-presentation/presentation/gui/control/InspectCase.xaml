<UserControl x:Class="checkin.presentation.gui.control.InspectCase"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="50">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="2*"></RowDefinition>
        </Grid.RowDefinitions>
        <!--
        <TextBlock Grid.Row="0" Style="{StaticResource CaseInspectName}" Text="{Binding CaseName, Mode=OneWay}"/>
        <TextBlock Grid.Row="1" Style="{StaticResource CaseDescription}" TextWrapping="Wrap" Text="{Binding Description, Mode=OneWay}"></TextBlock>
        <TextBlock Grid.Row="1" Style="{StaticResource ErrorMessageForUser}" Text="{Binding ErrorMessage, Mode=OneWay}"></TextBlock>
   -->
        </Grid>
</UserControl>
